# 博客维护行动清单

> 博客项目：`D:\Obsidian_note\blog`（VitePress）
> Obsidian 笔记源：`D:\Obsidian_note\Yclimb的知识库`
> 线上地址：https://Y-climb.github.io/blog/
> GitHub 仓库：https://github.com/Y-climb/blog

---

## 0. 核心概念（30 秒理解）

```
Obsidian 笔记 (.md)
      │  ① 复制到 blog/docs/posts/，转换格式，复制图片
      ▼
博客源文件 (docs/posts/xxx.md)
      │  ② npm run dev 本地预览验证
      ▼
git push 到 GitHub
      │  ③ GitHub Actions 自动构建部署（约 1~2 分钟）
      ▼
https://Y-climb.github.io/blog/
```

**所有操作都在 `D:\Obsidian_note\blog` 目录下执行命令。**

---

## 1. 格式转换规则（Obsidian → 博客）

从 Obsidian 复制笔记到 `docs/posts/` 后，必须做这三处转换：

### ① 图片语法

```markdown
❌ Obsidian 语法：![[Pasted image 20260910205337.png]]
✅ 博客语法：    ![描述文字](/images/mysql/Pasted%20image%2020260910205337.png)
```

- 图片文件复制到 `docs/public/images/<分类>/` 目录下
- 引用路径以 `/images/` 开头
- **文件名中的空格要替换成 `%20`**

### ② 批注语法

```markdown
❌ Obsidian 语法：%%这里是批注内容%%
✅ 博客语法（提示框）：
::: tip
这里是批注内容
:::
```

警告类内容用 `::: warning` 代替 `::: tip`。

### ③ 标题层级

Obsidian 里的 `**加粗当标题**` 建议改成标准 Markdown 标题 `## 标题`，这样博客右侧能自动生成目录。

---

## 2. 新增一篇文章（完整清单）

- [ ] **第 1 步**：把 Obsidian 笔记复制到博客 posts 目录

```bash
cp "D:/Obsidian_note/Yclimb的知识库/某笔记.md" "D:/Obsidian_note/blog/docs/posts/xxx.md"
```

- [ ] **第 2 步**：按第 1 节的规则转换格式（图片、批注、标题）
- [ ] **第 3 步**：复制笔记引用的图片

```bash
cp "D:/Obsidian_note/Yclimb的知识库/图片名.png" "D:/Obsidian_note/blog/docs/public/images/分类/"
```

- [ ] **第 4 步**：在文件顶部加上 Front Matter（文章元信息）

```markdown
---
title: 文章标题
date: 2026-09-19
tags:
  - 标签1
  - 标签2
---
```

- [ ] **第 5 步**：编辑 `docs/.vitepress/config.mjs`，在 sidebar 里注册文章

```js
{ text: '文章标题', link: '/posts/xxx' }
```

- [ ] **第 6 步**：本地预览验证（见第 5 节检查表）

```bash
cd D:/Obsidian_note/blog
npm run dev
# 浏览器打开 http://localhost:5173/blog/
```

- [ ] **第 7 步**：提交并推送，自动部署

```bash
git add -A
git commit -m "add: 文章标题"
git push
```

- [ ] **第 8 步**：等 1~2 分钟，访问线上地址确认（按 **Ctrl + F5** 强制刷新）

---

## 3. 修改一篇文章（完整清单）

- [ ] **第 1 步**：在 Obsidian 里修改笔记
- [ ] **第 2 步**：用修改后的笔记覆盖博客里的文件，并按第 1 节规则转换格式
- [ ] **第 3 步**：如果新增了图片，复制新图片到 `docs/public/images/`
- [ ] **第 4 步**：本地预览验证（dev 服务器运行时保存即热更新，无需重启）

```bash
cd D:/Obsidian_note/blog
npm run dev
```

- [ ] **第 5 步**：提交推送

```bash
git add -A
git commit -m "update: 文章标题 - 修改了什么"
git push
```

- [ ] **第 6 步**：等 1~2 分钟，线上 **Ctrl + F5** 确认更新生效

---

## 4. 删除一篇文章（完整清单）

删除必须动 **3 个地方**，缺一不可：

- [ ] **第 1 步**：删除文章文件

```bash
rm "D:/Obsidian_note/blog/docs/posts/xxx.md"
```

- [ ] **第 2 步**：编辑 `docs/.vitepress/config.mjs`，删掉 sidebar / nav 里对应的那一行
- [ ] **第 3 步**：删除该文章专用的图片（如果别的文章没在用）

```bash
rm "D:/Obsidian_note/blog/docs/public/images/分类/图片名.png"
```

- [ ] **第 4 步**：本地构建验证（重点：检查死链）

```bash
cd D:/Obsidian_note/blog
npm run build
```

> ✅ 预期：构建成功且无 dead link 报错。
> ❌ 如果报错说某处还链接到被删的文章，按提示把那个链接也删掉。

- [ ] **第 5 步**：提交推送

```bash
git add -A
git commit -m "remove: 文章标题"
git push
```

- [ ] **第 6 步**：等 1~2 分钟，确认：
  - 侧边栏里文章已消失
  - 直接访问文章原 URL 返回 **404**（删除生效的标志）

---

## 5. 本地验证检查表（每次改完都过一遍）

运行 `npm run dev`，打开 `http://localhost:5173/blog/`：

| 检查点 | 预期结果 |
|---|---|
| 左侧侧边栏 | 文章出现/消失符合预期 |
| 文章内容 | 标题、正文、代码块渲染正常 |
| 图片 | 全部能显示（不显示先检查路径里空格是否写成 `%20`） |
| 顶部搜索框 | 能搜到新增的文章 / 搜不到已删的文章 |
| 文章底部 | "上一篇 / 下一篇"翻页正常 |
| 右侧目录 | 章节标题层级正确 |

构建验证（部署前可选，能提前发现问题）：

```bash
npm run build
```

---

## 6. 完整流程测试方案（增 → 改 → 删 走一遍）

> 目的：用一篇临时测试文章，把三条链路全部验证一次。

### 第 1 步：创建测试文章

新建 `docs/posts/test.md`：

```markdown
---
title: 测试文章
---

# 测试文章

这是一篇用于测试发布/删除流程的文章。
```

在 `config.mjs` 的 sidebar 里加一行：`{ text: '测试文章', link: '/posts/test' }`

### 第 2 步：本地验证

`npm run dev` → 按第 5 节检查表逐项确认 ✅

### 第 3 步：修改测试

把正文改成"这是修改后的内容"，保存 → 浏览器自动热更新显示新内容 ✅

### 第 4 步：上线验证

```bash
git add -A && git commit -m "test: add test post" && git push
```

等 2 分钟 → 访问 `https://Y-climb.github.io/blog/posts/test.html` 能打开 ✅

### 第 5 步：删除测试

```bash
rm docs/posts/test.md
# 并删掉 config.mjs 里那行 sidebar 条目
npm run build   # 确认无死链报错
```

### 第 6 步：线上删除验证

```bash
git add -A && git commit -m "test: remove test post" && git push
```

等 2 分钟 → 侧边栏无此文 ✅；访问原 URL 显示 404 ✅

---

## 7. 常见问题

| 问题 | 解决办法 |
|---|---|
| `npm install` 报 ECONNRESET | 加镜像源：`npm install --registry=https://registry.npmmirror.com` |
| 线上看不到更新 | 等够 2 分钟，然后 **Ctrl + F5** 强制刷新 |
| 图片显示不出来 | 检查路径：以 `/images/` 开头、空格写成 `%20`、文件确实复制到了 `docs/public/images/` |
| 构建报 dead link | 有地方还链着不存在的文章，按报错提示删掉链接 |
| 想看部署进度/失败原因 | 打开 https://github.com/Y-climb/blog/actions |
| 域名 404 | 确认 Settings → Pages → Source 选的是 **GitHub Actions** |

---

## 8. 常用命令速查

```bash
cd D:/Obsidian_note/blog    # 进入项目
npm run dev                 # 本地预览 → http://localhost:5173/blog/
npm run build               # 构建验证（查死链）
npm run preview             # 预览构建产物

git add -A                  # 暂存所有改动
git commit -m "说明"        # 提交
git push                    # 推送 → 自动部署
git status                  # 查看当前改动
git log --oneline           # 查看提交历史
```
